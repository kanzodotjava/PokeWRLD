import { PokemonTypeBackgroundPipe } from "./pokemontypebackground.pipe";

describe('PokemonTypeBackgroundPipe', () => {
  it('create an instance', () => {
    const pipe = new PokemonTypeBackgroundPipe();
    expect(pipe).toBeTruthy();
  });
});
